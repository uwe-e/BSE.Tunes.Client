<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BSE.Tunes.WinUI.Client.Controls.RandomPlayerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BSE.Tunes.WinUI.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Button x:Name="ButtonPlayRandom"
                Command="{x:Bind ViewModel.ButtonPlayRandomCommand}"
                HorizontalAlignment="Stretch"
                Height="60"
                Background="{ThemeResource SystemAccentColor}"
                HorizontalContentAlignment="Center"
                Foreground="{ThemeResource SystemChromeWhiteColor}">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <FontIcon Glyph="&#xE946;" FontSize="28"/>
                    <!--<FontIcon FontSize="16" Glyph="&#xE8C9;"  />
                    <FontIcon FontSize="26" Glyph="&#xEA3A;" FontWeight="SemiBold" />-->
                </Grid>
                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.Text, Mode=OneWay}" Style="{ThemeResource BodyLargeStrongTextBlockStyle}"
                           VerticalAlignment="Center" TextWrapping="Wrap" TextTrimming="WordEllipsis" Margin="10,0,0,0"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>
