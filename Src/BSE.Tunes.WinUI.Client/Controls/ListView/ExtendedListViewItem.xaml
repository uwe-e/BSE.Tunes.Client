<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:BSE.Tunes.WinUI.Client.Controls">

    <!--<Style TargetType="controls:ExtendedListView" BasedOn="{StaticResource DefaultListViewStyle}"/>-->


    <Style TargetType="controls:ExtendedListViewItem">
        <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
        <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{ThemeResource ListViewItemForeground}"/>
        <Setter Property="TabNavigation" Value="Local"/>
        <Setter Property="IsHoldingEnabled" Value="True"/>
        <Setter Property="Padding" Value="12,0,12,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="MinWidth" Value="{ThemeResource ListViewItemMinWidth}"/>
        <Setter Property="MinHeight" Value="{ThemeResource ListViewItemMinHeight}"/>
        <Setter Property="AllowDrop" Value="False"/>
        <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}"/>
        <Setter Property="FocusVisualMargin" Value="0"/>
        <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:ExtendedListViewItem">
                    <Grid x:Name="ContentBorder"
      Background="{TemplateBinding Background}"
      BorderBrush="{TemplateBinding BorderBrush}"
      BorderThickness="{TemplateBinding BorderThickness}"
      CornerRadius="{TemplateBinding CornerRadius}"
      Control.IsTemplateFocusTarget="True">

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="BorderBackground.Fill" Value="{ThemeResource ListViewItemBackgroundPointerOver}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPointerOver}"/>
                                        <Setter Target="PreSelectionSquare.Visibility" Value="Visible"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="BorderBackground.Fill" Value="{ThemeResource ListViewItemBackgroundPressed}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundPressed}"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Target="BorderBackground.Fill" Value="{ThemeResource ListViewItemBackgroundSelected}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelected}"/>
                                        <Setter Target="MultiSelectCheck.Opacity" Value="1"/>
                                        <Setter Target="MultiSelectSquare.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="PointerOverSelected">
                                    <VisualState.Setters>
                                        <Setter Target="BorderBackground.Fill" Value="{ThemeResource ListViewItemBackgroundSelectedPointerOver}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelectedPointerOver}"/>
                                        <Setter Target="MultiSelectCheck.Opacity" Value="1"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="PressedSelected">
                                    <VisualState.Setters>
                                        <Setter Target="BorderBackground.Fill" Value="{ThemeResource ListViewItemBackgroundSelectedPressed}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ListViewItemForegroundSelectedPressed}"/>
                                        <Setter Target="MultiSelectCheck.Opacity" Value="1"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="DisabledStates">
                                <VisualState x:Name="Enabled">
                                    <VisualState.Setters>
                                        <Setter Target="PreSelectionSquare.Visibility" Value="Collapsed"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="ContentBorder.Opacity" Value="{ThemeResource ListViewItemDisabledThemeOpacity}"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="MultiSelectStates">
                                <VisualState x:Name="MultiSelectDisabled">
                                    <VisualState.Setters>
                                        <Setter Target="MultiSelectSquare.Visibility" Value="Collapsed"/>
                                        <Setter Target="PreSelectionSquare.Visibility" Value="Collapsed"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="MultiSelectEnabled">
                                    <VisualState.Setters>
                                        <Setter Target="MultiSelectSquare.Visibility" Value="Visible"/>
                                        <Setter Target="MultiSelectCheck.Visibility" Value="Visible"/>
                                        <Setter Target="ContentPresenterGrid.Margin" Value="32,0,0,0"/>
                                        <Setter Target="PreSelectionSquare.Visibility" Value="Collapsed"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="PreSelectionStates">
                                <VisualState x:Name="EnablePreSelection">
                                    <VisualState.Setters>
                                        <Setter Target="PlaceholderPreSelection.Visibility" Value="Visible"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="DisablePreSelection">
                                    <VisualState.Setters>
                                        <Setter Target="PlaceholderPreSelection.Visibility" Value="Collapsed"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Rectangle x:Name="BorderBackground"
                                   Fill="Transparent"
                                   IsHitTestVisible="False"
                                   Control.IsTemplateFocusTarget="True"/>

                        <Grid x:Name="ContentPresenterGrid" Background="Transparent" Margin="32,0,0,0">
                            <ContentPresenter x:Name="ContentPresenter"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Content="{TemplateBinding Content}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>

                        <!-- Pre-Selection Icon -->
                        <Grid x:Name="PlaceholderPreSelection" Visibility="Collapsed">
                            <!-- The PreSelector should be switchable.
                                This is the element that makes the visiblity -->
                            <Button Opacity="0" Width="30" VerticalAlignment="Stretch" BorderBrush="{x:Null}">
                                <!--The PreSelectionSquare isnt visible when pointerover on rows with an alternating color.
                                    this fake button as the parent element prevents this-->
                            </Button>

                            <Border x:Name="PreSelectionSquare"
                                    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    HorizontalAlignment="Left"
                                    Height="20"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center"
                                    Visibility="Collapsed"
                                    Width="20">
                                <Border.Clip>
                                    <RectangleGeometry Rect="0,0,20,20">
                                    </RectangleGeometry>
                                </Border.Clip>
                                <FontIcon x:Name="PreSelectCheck"
                                          Foreground="{ThemeResource SystemControlForegroundBaseMediumHighBrush}"
                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                          FontSize="16" Glyph="&#xE73E;" Opacity="0" Visibility="Visible"/>
                                <!--<FontIcon x:Name="PreSelectCheck"
                                          Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                          FontSize="16"
                                          Glyph="&#xE73E;"
                                          Visibility="Visible"/>-->
                            </Border>
                        </Grid>

                        <!-- Multi-Select Checkbox -->
                        <Border x:Name="MultiSelectSquare"
                                BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                BorderThickness="1"
                                CornerRadius="2"
                                HorizontalAlignment="Left"
                                Height="20"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                Visibility="Collapsed"
                                Width="20">
                            <FontIcon x:Name="MultiSelectCheck"
                                      Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                      FontSize="16"
                                      Glyph="&#xE73E;"
                                      Opacity="0"
                                      Visibility="Collapsed"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
