<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BSE.Tunes.WinUI.Client.Controls.FeaturedPlaylistView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BSE.Tunes.WinUI.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BSE.Tunes.WinUI.Client.Models"
    xmlns:controls="using:BSE.Tunes.WinUI.Client.Controls"
    mc:Ignorable="d">

    <StackPanel>
        <HyperlinkButton x:Uid="FeaturedPlaylistView_Title" Content="Playlists"
                         Style="{StaticResource HeaderedSectionHeader}"/>
        <Grid>
            <controls:ItemsCarousel ItemsSource="{x:Bind ViewModel.Items}"
                            ItemClickCommand="{x:Bind ViewModel.SelectItemCommand}"
                            HorizontalScrollBarVisibility="Hidden"
                            ItemTemplate="{StaticResource SmallImagePanelTemplate}"/>

            <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                      Width="40"
                      Height="40"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
        </Grid>
    </StackPanel>
</UserControl>
