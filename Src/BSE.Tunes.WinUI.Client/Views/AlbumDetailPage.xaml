<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BSE.Tunes.WinUI.Client.Views.AlbumDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BSE.Tunes.WinUI.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BSE.Tunes.WinUI.Client.Models"
    xmlns:controls="using:BSE.Tunes.WinUI.Client.Controls"
    xmlns:behaviors="using:BSE.Tunes.WinUI.Client.Behaviors"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="TrackItemTemplate" x:DataType="models:TrackItem">
            <UserControl>
                <Grid x:Name="listViewItem" Height="44">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="{x:Bind TrackNumber, Mode=OneWay}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="1" 
                               Text="{x:Bind Title, Mode=OneWay}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               Margin="12,0,0,0"/>

                    <TextBlock Grid.Column="2" 
                               Text="{x:Bind Artist, Mode=OneWay}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               Margin="12,0,0,0"/>

                    <TextBlock Grid.Column="3" 
                               Text="{x:Bind FormattedDuration, Mode=OneWay}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Margin="12,0,0,0"/>
                </Grid>
            </UserControl>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Sticky header wrapped in UserControl to enable VisualStateManager support.
            VisualStates are defined on the inner Grid and controlled by StickyHeaderBehavior.
            When scrolling down, 'Collapsed' state hides subtitle/genre and shows action buttons
            When scrolling up, 'Expanded' state shows subtitle/genre and hides action buttons. -->
        <UserControl x:Name="infoHeader"
                     Grid.Row="0">

            <Grid Padding="5" Background="{ThemeResource LayerFillColorDefaultBrush}">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="ScrollStates">
                        <VisualState x:Name="Expanded">
                            <VisualState.Setters>
                                <Setter Target="infoSubTitle.Visibility" Value="Visible"/>
                                <Setter Target="infoSubTitle.Opacity" Value="1"/>
                                <Setter Target="albumGenre.Visibility" Value="Visible"/>
                                <Setter Target="albumGenre.Opacity" Value="1"/>
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Collapsed">
                            <VisualState.Setters>
                                <Setter Target="infoSubTitle.Visibility" Value="Collapsed"/>
                                <Setter Target="infoSubTitle.Opacity" Value="0"/>
                                <Setter Target="albumGenre.Visibility" Value="Collapsed"/>
                                <Setter Target="albumGenre.Opacity" Value="0"/>
                                <!--<Setter Target="infoAction.VerticalAlignment" Value="Top"/>-->
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image x:Name="infoCover" 
                   Grid.Column="0"
                   Source="{x:Bind ViewModel.CoverImageSource, Mode=OneWay}"
                   Width="240"
                   Height="240"
                   Margin="10"
                   Stretch="UniformToFill"/>

                <Grid x:Name="infoProperties" 
                  Grid.Column="1"
                  Margin="10" 
                  VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="infoTitle" 
                           Grid.Row="0"
                           Text="{x:Bind ViewModel.Album.Title, Mode=OneWay}"
                           Style="{StaticResource TitleLargeTextBlockStyle}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap"/>

                    <TextBlock x:Name="infoSubTitle" 
                           Grid.Row="1"
                               Margin="0,8,0,8"
                           Text="{x:Bind ViewModel.Album.Artist.Name, Mode=OneWay}"
                           Style="{StaticResource BodyLargeTextBlockStyle}"
                           TextTrimming="CharacterEllipsis"/>

                    <TextBlock x:Name="albumGenre" 
                           Grid.Row="2"
                               Margin="0,8,0,8"
                           Style="{StaticResource BodyTextBlockStyle}"
                           TextTrimming="CharacterEllipsis">
                        <Run Text="{x:Bind ViewModel.Album.Year, Mode=OneWay}"/>
                        <Run Text="•"/>
                        <Run Text="{x:Bind ViewModel.Album.Genre.Name, Mode=OneWay}"/>
                    </TextBlock>

                    <StackPanel x:Name="infoAction" 
                            Grid.Row="3"
                                Margin="0,4,0,0"
                            Orientation="Horizontal">

                        <Button Margin="0,0,8,0" Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE768;" FontSize="16"/>
                                <TextBlock Text="Play All"/>
                            </StackPanel>
                        </Button>

                        <Button Margin="8,0">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE8B1;" FontSize="16"/>
                                <TextBlock Text="Shuffle"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="buttonAddAllTo" Margin="8,0">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE710;"/>
                                <TextBlock Text="Add To"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

            </Grid>
        </UserControl>

        <!-- CommandBar -->
        <CommandBar x:Name="stickyCommandBar"
                    Grid.Row="1"
                    VerticalAlignment="Top"
                    DefaultLabelPosition="Right"
                    Visibility="{x:Bind ViewModel.IsCommandBarVisible, Mode=OneWay}">
            <AppBarButton Icon="Cancel" Label="Clear Selection" />
            <AppBarButton Icon="Play" Label="Play Selection" />
            <AppBarButton Icon="Next" Label="Play Next" />
            <AppBarButton Icon="Add" Label="Add to Playlist"/>
        </CommandBar>

        <!-- ListView with Behavior
        
        -->
        <controls:ExtendedListView x:Name="listview"
                                   Grid.Row="1"
                                   ItemTemplate="{StaticResource TrackItemTemplate}"
                                   ItemsSource="{x:Bind ViewModel.Tracks, Mode=OneWay}"
                                   EnablePreSelection="True"
                                   SelectionMode="{x:Bind ViewModel.SelectionMode, Mode=OneWay}"
                                   AlternatingRow="{ThemeResource SubtleFillColorSecondaryBrush}"
                                   SelectedItem="{x:Bind ViewModel.SelectedTrack, Mode=TwoWay}"
                                   controls:ListViewExtensions.SelectedItems="{x:Bind ViewModel.SelectedItems, Mode=TwoWay}"
                                   IsItemClickEnabled="{x:Bind ViewModel.IsItemClickEnabled, Converter={StaticResource BoolNegationConverter}}"
                                   PreSelectionCommand="{x:Bind ViewModel.SelectItemsCommand}"
                                   Padding="0,48,0,0">
            <interactivity:Interaction.Behaviors>
                <behaviors:StickyHeaderBehavior 
                    HeaderElement="{x:Bind infoHeader}"
                    CoverImage="{x:Bind infoCover}"
                    MaxHeaderHeight="240"
                    MinHeaderHeight="120"
                    MaxCoverSize="220"
                    MinCoverSize="100"
                    ScrollThreshold="160"/>
            </interactivity:Interaction.Behaviors>
        </controls:ExtendedListView>
    </Grid>
</Page>
