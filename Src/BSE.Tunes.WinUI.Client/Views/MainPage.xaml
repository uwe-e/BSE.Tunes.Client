<Page
    x:Class="BSE.Tunes.WinUI.Client.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:BSE.Tunes.WinUI.Client.Views"
    xmlns:uc="using:BSE.Tunes.WinUI.Client.Controls"
    xmlns:behaviors="using:BSE.Tunes.WinUI.Client.Behaviors"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid>

        <RefreshContainer x:Name="ContentArea" Margin="-56, -24, -56, 0">
            <interactivity:Interaction.Behaviors>
                <behaviors:RefreshContainerBehavior Command="{x:Bind ViewModel.RefreshCommand}" />
            </interactivity:Interaction.Behaviors>

            <ScrollViewer Padding="0">
                <RelativePanel x:Name="hubContainer" EntranceNavigationTransitionInfo.IsTargetElement="True"
                           RelativePanel.AlignTopWithPanel="True"
                           RelativePanel.AlignLeftWithPanel="True"
                           RelativePanel.AlignRightWithPanel="True">

                    <Grid x:Name="flipView" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignTopWithPanel="True" >
                        <uc:AlbumsCarouselView/>
                    </Grid>

                    <Grid x:Name="randomPlayer" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="flipView"
                      Margin="20,20,20,0">
                        <uc:RandomPlayerView  />
                    </Grid>

                    <Grid x:Name="featuredAlbums" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="randomPlayer"
                      Margin="20,20,20,0">
                        <uc:FeaturedAlbumsView/>
                    </Grid>

                    <Grid x:Name="featuredPaylists" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="featuredAlbums"
                          Margin="20,20,20,0">
                        <uc:FeaturedPlaylistView/>
                    </Grid>
                </RelativePanel>
            </ScrollViewer>
        </RefreshContainer>
        <Button x:Uid="MainPage_RefreshButton" HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="0,-24,-56,0" 
                Command="{x:Bind ViewModel.RefreshCommand}"
                Content="&#xE72C;" 
                FontFamily="Segoe MDL2 Assets"
                ToolTipService.ToolTip="Refresh"
                Foreground="{ThemeResource SystemChromeWhiteColor}"
                Background="{ThemeResource SystemAccentColor}"/>

    </Grid>
</Page>
