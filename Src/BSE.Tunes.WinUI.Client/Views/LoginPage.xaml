<Page
    x:Class="BSE.Tunes.WinUI.Client.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="440" Spacing="16" Padding="24">

      <TextBlock x:Uid="LoginPage_Title" Text="Login"
                 FontSize="28"
                 FontWeight="SemiBold"
                 Margin="0,0,0,16"/>

      <TextBox x:Uid="LoginPage_UserNameTextBox" x:Name="UserNameTextBox"
                 Header="Username"
                 PlaceholderText="Enter your username"
                 Text="{x:Bind ViewModel.UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

      <PasswordBox x:Uid="LoginPage_PasswordBox"  x:Name="PasswordBox"
                   Header="Password"
                   PlaceholderText="Enter your password"
                   Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

      <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                 Foreground="Red"
                 TextWrapping="Wrap"
                 Visibility="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}"/>

      <Button Command="{x:Bind ViewModel.LoginCommand}"
              HorizontalAlignment="Stretch"
              Margin="0,8,0,0"
              IsEnabled="{x:Bind ViewModel.IsLoggingIn, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
        <Button.Content>
          <StackPanel Orientation="Horizontal" Spacing="8">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoggingIn, Mode=OneWay}"
                          Width="16" Height="16"
                          Visibility="{x:Bind ViewModel.IsLoggingIn, Mode=OneWay}"/>
            <TextBlock x:Uid="LoginPage_SignInButton_Text" Text="Sign In"/>
          </StackPanel>
        </Button.Content>
      </Button>
    </StackPanel>
  </Grid>
</Page>